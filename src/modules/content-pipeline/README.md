# content-pipeline

SPEC `docs/SPEC.md` §5.2 · checklist group 7.3.

Content items inside a project with the full field set from §4 (deadline,
assignee, script/video URLs, topic, customer research URL, evaluation), table +
Kanban views, filtering/sorting, and free-text comments with @mention.

Layout: `components/` `hooks/` `services/` `types/`.
